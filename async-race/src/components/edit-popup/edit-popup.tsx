import React, { useEffect, useState } from "react";

export default function EditPopup() {
    return (
        <div>
            <input type="text" />
            <input type="color" />
            <button>Ok</button>
            <button>Cancel</button>
        </div>
    )
}